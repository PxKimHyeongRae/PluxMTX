# NACK 인터셉터 제거 구현 계획

## 목표
WebRTC NACK (Negative Acknowledgement) 인터셉터를 제거하여 CPU 사용량 20-30% 감소, 메모리 사용량 70% 감소를 달성합니다.

## 분석 결과 요약

### 현재 상황
- **메모리 오버헤드**: 91.63 MB (전체 힙의 72%)
- **CPU 오버헤드**: RTP 쓰기 경로의 주요 병목
- **원인**: NACK 메커니즘이 모든 RTP 패킷을 버퍼에 저장하여 손실 시 재전송 준비

### NACK란?
- WebRTC에서 패킷 손실을 감지하고 재전송을 요청하는 메커니즘
- 수신자가 패킷 손실을 감지하면 송신자에게 NACK 메시지 전송
- 송신자는 버퍼에 저장된 패킷을 재전송
- 네트워크 품질이 좋지 않은 환경에서 유용하지만, 안정적인 환경에서는 오버헤드만 발생

## 영향 받는 파일

### 1. internal/protocols/webrtc/peer_connection.go
- **위치**: 라인 70-73
- **현재 코드**: `webrtc.ConfigureNack()` 호출
- **변경 필요**: 해당 라인 주석 처리 또는 조건부 실행

## 구현 방안

### 옵션 A: 직접 제거 (권장)

**장점**:
- 즉시 효과 확인 가능
- 코드가 간단하고 명확

**단점**:
- 런타임에 설정 변경 불가
- 재활성화 시 재컴파일 필요

**구현**:
```go
// internal/protocols/webrtc/peer_connection.go

func registerInterceptors(
	mediaEngine *webrtc.MediaEngine,
	interceptorRegistry *interceptor.Registry,
	onStatsInterceptor func(s *statsInterceptor),
) error {
	// NACK 인터셉터 비활성화
	// 프로파일링 결과: 메모리 91.63MB, CPU 20-30% 오버헤드
	// 안정적인 네트워크 환경에서는 불필요한 오버헤드
	// err := webrtc.ConfigureNack(mediaEngine, interceptorRegistry)
	// if err != nil {
	// 	return err
	// }

	err := webrtc.ConfigureSimulcastExtensionHeaders(mediaEngine)
	if err != nil {
		return err
	}

	err = webrtc.ConfigureTWCCSender(mediaEngine, interceptorRegistry)
	if err != nil {
		return err
	}

	interceptorRegistry.Add(&statsInterceptorFactory{
		onCreate: onStatsInterceptor,
	})

	return nil
}
```

### 옵션 B: 설정 파일 기반 제어 (권장하지 않음)

**장점**:
- 런타임에 설정 변경 가능 (재시작 필요)
- 필요 시 쉽게 재활성화

**단점**:
- 구현이 복잡 (설정 추가, 파싱, 전달 등)
- 설정 항목이 증가하여 혼란 가능성

**구현 단계** (참고용):
1. `internal/conf/conf.go`에 설정 추가
2. `mediamtx.yml` 스키마 업데이트
3. `peer_connection.go`에 조건문 추가

**예시**:
```go
// conf.go에 추가
type Conf struct {
	// ...
	WebRTCDisableNACK bool `json:"webrtcDisableNACK"`
	// ...
}

// peer_connection.go 수정
func registerInterceptors(
	mediaEngine *webrtc.MediaEngine,
	interceptorRegistry *interceptor.Registry,
	onStatsInterceptor func(s *statsInterceptor),
	disableNACK bool, // 새 파라미터
) error {
	if !disableNACK {
		err := webrtc.ConfigureNack(mediaEngine, interceptorRegistry)
		if err != nil {
			return err
		}
	}
	// ...
}
```

## 권장 구현: 옵션 A (직접 제거)

안정적인 서버 환경에서는 NACK의 이점이 거의 없으므로 직접 제거를 권장합니다.

## 구현 체크리스트

### Phase 1: 코드 변경
- [ ] `internal/protocols/webrtc/peer_connection.go` 백업
- [ ] `registerInterceptors` 함수에서 `ConfigureNack` 호출 주석 처리
- [ ] 주석에 제거 이유와 성능 데이터 기록
- [ ] 코드 리뷰 및 검증

### Phase 2: 빌드 및 테스트
- [ ] 프로젝트 빌드
  ```bash
  go build -o mediamtx.exe
  ```
- [ ] 빌드 성공 확인
- [ ] 기본 기능 테스트 (스트림 1개로 시작)
  - [ ] WebRTC 연결 성공
  - [ ] 비디오 스트리밍 정상 작동
  - [ ] 오디오 스트리밍 정상 작동

### Phase 3: 성능 테스트
- [ ] 테스트 환경 구성 (64개 스트림)
- [ ] CPU 사용량 모니터링 (30분 이상)
- [ ] 메모리 사용량 모니터링
- [ ] 프로파일 데이터 수집
  ```bash
  curl http://localhost:9999/debug/pprof/profile?seconds=60 > cpu_after_nack_removal.prof
  curl http://localhost:9999/debug/pprof/heap > heap_after_nack_removal.prof
  curl http://localhost:9999/debug/pprof/goroutine > goroutine_after_nack_removal.prof
  ```
- [ ] 비교 분석
  ```bash
  go tool pprof -top -cum cpu_after_nack_removal.prof
  go tool pprof -text heap_after_nack_removal.prof | head -60
  ```

### Phase 4: 품질 검증
- [ ] 비디오 품질 확인 (시각적 검사)
- [ ] 프레임 드롭 모니터링
- [ ] 패킷 손실률 확인 (WebRTC stats)
- [ ] 지연 시간(latency) 측정
- [ ] 네트워크 대역폭 사용량 확인

### Phase 5: 프로덕션 배포
- [ ] 스테이징 환경에서 7일 이상 안정성 테스트
- [ ] 롤백 계획 수립
- [ ] 프로덕션 배포
- [ ] 배포 후 모니터링 (24시간 집중 모니터링)

## 예상 결과

### 성능 개선
| 항목 | 현재 | 예상 | 개선율 |
|------|------|------|--------|
| 힙 메모리 | 127 MB | 35-40 MB | -70% |
| PacketFactory 메모리 | 91.63 MB | < 5 MB | -95% |
| CPU (RTP 경로) | 43.11% | 25-30% | -30-40% |
| 전체 CPU | 높음 | 중간 | -20-30% |

### 부작용 (예상)
- **패킷 손실 재전송 없음**: 네트워크 불안정 시 품질 저하 가능
  - 완화: 안정적인 네트워크 환경 사용
  - 모니터링: 패킷 손실률 지속 관찰
- **초기 연결 품질 영향**: Wi-Fi 등 불안정한 연결에서 버퍼링 증가 가능
  - 완화: 유선 연결 권장

## 테스트 시나리오

### 1. 정상 네트워크 환경
- **목표**: 기본 기능 검증
- **방법**: 로컬 네트워크에서 64개 스트림 동시 재생
- **기대**: 품질 저하 없이 성능 개선

### 2. 패킷 손실 시뮬레이션 (선택적)
- **목표**: NACK 제거의 영향 측정
- **방법**: `tc` (traffic control)로 1% 패킷 손실 유도
  ```bash
  # Linux에서 실행
  tc qdisc add dev eth0 root netem loss 1%
  ```
- **기대**: 약간의 품질 저하는 있지만 허용 가능한 수준

### 3. 부하 테스트
- **목표**: 다수 클라이언트 연결 시 안정성 검증
- **방법**: 100개 이상 동시 연결
- **기대**: 메모리/CPU 안정적 유지

## 모니터링 지표

### 성능 지표
```bash
# CPU 사용률
top -p $(pgrep mediamtx) -b -n 1 | grep mediamtx

# 메모리 사용량
ps aux | grep mediamtx | awk '{print $6}'

# 고루틴 수
curl -s http://localhost:9999/debug/pprof/goroutine?debug=1 | head -1
```

### WebRTC 통계 (브라우저 개발자 도구)
```javascript
// Chrome DevTools Console
pc.getStats().then(stats => {
  stats.forEach(report => {
    if (report.type === 'inbound-rtp') {
      console.log('Packets Lost:', report.packetsLost);
      console.log('Packets Received:', report.packetsReceived);
      console.log('Loss Rate:', (report.packetsLost / report.packetsReceived * 100).toFixed(2) + '%');
    }
  });
});
```

### 품질 지표 목표
- **패킷 손실률**: < 0.1% (안정적인 네트워크)
- **프레임 드롭**: < 1% (시각적 영향 최소화)
- **지연 시간 증가**: < 50ms (허용 범위)

## 롤백 계획

### 롤백 트리거
다음 중 하나 발생 시 롤백:
1. 패킷 손실률 > 1% (지속적)
2. 사용자 불만 접수 (화질 저하)
3. CPU/메모리 개선 효과 미미 (< 10%)
4. 예상치 못한 버그 발생

### 롤백 절차
1. 백업된 원본 코드로 복원
   ```bash
   git checkout internal/protocols/webrtc/peer_connection.go
   ```
2. 재빌드
   ```bash
   go build -o mediamtx.exe
   ```
3. 서비스 재시작
4. 모니터링 확인

### 빠른 롤백 (긴급)
```bash
# 이전 바이너리로 교체
cp mediamtx.exe.backup mediamtx.exe
systemctl restart mediamtx  # 또는 해당 서비스 재시작 명령
```

## 추가 최적화 (NACK 제거 후)

NACK 제거 후 여전히 성능이 부족하다면:

### 1. mDNS 비활성화 (추가 15-20% CPU 감소)
```go
// peer_connection.go의 Start() 함수 내부
settingsEngine.SetICEMulticastDNSMode(ice.MulticastDNSModeDisabled)
```

### 2. RTCP 리포트 간격 증가 (추가 5-10% CPU 감소)
```go
// outgoing_track.go
Period: 3 * time.Second, // 1초 → 3초
```

### 3. 동시 스트림 수 제한
- API를 통해 동시 연결 수 제한
- 자원 부족 시 새 연결 거부

## 타임라인

| 일정 | 단계 | 소요 시간 |
|------|------|-----------|
| D+0 | 코드 변경 및 빌드 | 30분 |
| D+0 | 기본 기능 테스트 | 1시간 |
| D+0 ~ D+1 | 성능 테스트 (24시간) | 1일 |
| D+1 | 결과 분석 및 검증 | 2시간 |
| D+2 ~ D+8 | 스테이징 안정성 테스트 | 7일 |
| D+9 | 프로덕션 배포 | 1시간 |
| D+9 ~ D+10 | 집중 모니터링 | 1일 |

**총 소요 기간**: 약 10일

## 성공 기준

### 필수 기준 (모두 충족 필요)
- [ ] CPU 사용량 20% 이상 감소
- [ ] 메모리 사용량 50% 이상 감소
- [ ] 패킷 손실률 1% 미만 유지
- [ ] 비디오 품질 저하 없음 (시각적 확인)
- [ ] 7일 이상 안정적 운영

### 목표 기준 (권장)
- [ ] CPU 사용량 30% 이상 감소
- [ ] 메모리 사용량 70% 이상 감소
- [ ] 패킷 손실률 0.5% 미만 유지
- [ ] 30일 이상 안정적 운영

## 위험도 평가

| 위험 요소 | 확률 | 영향도 | 완화 방안 |
|-----------|------|--------|-----------|
| 패킷 손실 증가 | 낮음 | 중간 | 네트워크 품질 모니터링, 롤백 준비 |
| 품질 저하 불만 | 낮음 | 높음 | 사전 테스트, 단계적 배포 |
| 예상치 못한 버그 | 매우 낮음 | 높음 | 철저한 테스트, 롤백 계획 |
| 성능 개선 미달 | 매우 낮음 | 낮음 | 프로파일링 데이터로 검증됨 |

**전체 위험도**: 🟢 낮음

## 참고 자료

### WebRTC NACK 관련
- [RFC 4585 - Extended RTP Profile for RTCP-Based Feedback](https://tools.ietf.org/html/rfc4585)
- [Pion WebRTC - Interceptors](https://github.com/pion/interceptor)
- [WebRTC Glossary - NACK](https://webrtcglossary.com/nack/)

### 성능 최적화
- [Go Profiling](https://go.dev/blog/pprof)
- [Pion WebRTC Performance Guide](https://github.com/pion/webrtc/wiki/Performance)

## 결론

NACK 인터셉터 제거는:
- ✅ **고효과**: 메모리 70%, CPU 20-30% 감소 예상
- ✅ **저위험**: 안정적인 네트워크 환경에서 부작용 최소
- ✅ **빠른 구현**: 1줄 주석 처리로 즉시 적용 가능
- ✅ **쉬운 롤백**: 문제 발생 시 즉시 복구 가능

**권장 사항**: 즉시 구현 및 테스트를 진행하고, 성능 개선 효과를 확인한 후 프로덕션에 배포하세요.
